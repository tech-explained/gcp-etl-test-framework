"""Metadata-driven ETL test framework: generic validators for GCS -> Dataflow -> Postgres pipelines.

Importing this package has no Airflow / GCP dependencies so validators can be
unit-tested anywhere.
"""
